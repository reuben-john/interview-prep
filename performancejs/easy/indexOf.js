// indexOf - Implement the indexOf function for arrays.

function indexOf(array) {}

import test from "ava";

test("test", t => t.is(indexOf()), true);

indexOf([1, 2, 3], 1); // 0
indexOf([1, 2, 3], 4); // -1
